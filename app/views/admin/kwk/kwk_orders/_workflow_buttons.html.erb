<% @order.current_state.events.each do |state| %>
  <%= link_to state[1][0].name.to_s.titleize, transition_workflow_admin_kwk_order_path(@order, event: state[1][0].name), class: "btn btn-default-o btn-xs btn-#{state[1][0].name}", remote: true, method: :post %>
<% end %>