<% provide :meta_title, 'Inquiries' %>

<div class="panel panel-default">
  <div class="panel-heading">
    <ul class="list-inline">
      <li data-toggle="tooltip" data-original-title="Search for anything">
        <%= form_tag admin_inquiries_url, method: :get, class: 'form-inline' do %>          
          <div class="form-group">
            <div class="input-group">
              <div class="input-group-addon"><%= icon 'search' %></div>
              <%= text_field_tag :keyword, params[:keyword], class: 'form-control input-sm', placeholder: 'Search for anything' %>
            </div>
          </div>
        <% end %>
      </li>
      <li>
        <%= link_to icon('times', 'Reset'), admin_inquiries_url if params[:keyword].present? %>
      </li>
    </ul>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th width="50%">Inquiry</th>
          <th width="50%">Preview</th>
        </tr>
      </thead>
      <tbody>
        <%= render @inquiries %>
      </tbody>
    </table>
  </div>
</div>

<%= paginate @inquiries %>